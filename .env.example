# Cloud-sync backend URL (Cloudflare Worker, see ./worker/). When unset, the
# app runs in local-only mode (IndexedDB only, no cloud backup UI). Public —
# this URL ships to every browser, it's not a secret.
PUBLIC_CLOUD_API_URL=https://fallout-sync.matpb.workers.dev
